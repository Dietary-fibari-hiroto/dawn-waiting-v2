---
import { ImagesIndex } from "../../../assets/ImagesIndex"

const hero = `url(${ImagesIndex.hero})`
const heroSp = `url(${ImagesIndex.verticalHero})`
---

<section class="section-frame__d hero">
  <div class="hero-frame section-frame__d"/>
    <div class="hero-text">
        <p class="text-7xl-bold">OurVibe</p>
        <p class="text-xl">by team DAYOFF.</p>
    </div>
    <p class="text-5xl-bold hero-text line-concept">
        そばにいても、<br/>
離れていても、<br/>
私たちで作る、<br/>
私たちだけの音楽体験<br/>
    </p>


  <p class="hero-meta">卒業制作 Team DayOff : 加藤昌輝,小野寺広登,石塚直樹,朴建泳  - 発表資料</p>
</section>

<style define:vars={{ hero, heroSp }} lang="scss">
.hero {
  background-image: var(--hero);
  position: relative;
  box-sizing: border-box;
  padding: 3rem;

  display: flex;
  justify-content: space-evenly;
  align-items: center;

  background-position: center;
  background-size: cover;
  background-repeat: no-repeat;

  &-text{
    color:white;
}

  &-frame {
    position: absolute;
    box-sizing: border-box;
    top: 0;
    left: 0;
    border: solid 3rem #eeeeee;
  }

  &-meta {
    position: absolute;
    left: 50%;
    bottom: 0;
    transform: translateX(-50%);
    padding: 1rem;
    width:100%;
    box-sizing: border-box;
    text-align: center;
  }
}

@media (max-width: 767px) {
  .hero {
    background-image: var(--heroSp);
    flex-direction: column;
  }
}
</style>
