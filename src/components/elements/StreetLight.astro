<div class="streetlamp-container">
        <!-- 光 -->
        <div class="light-glow"></div>
        <div class="ground-light"></div>
        
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>

        <!-- 星 -->
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>

        <!-- 街灯 -->
        <svg class="streetlamp" viewBox="0 0 200 300" xmlns="http://www.w3.org/2000/svg">
            <g class="pole">
                <!-- ポール -->
                <rect x="96" y="80" width="8" height="200" fill="#2a2a2a" stroke="#1a1a1a" stroke-width="1"/>
                <rect x="94" y="278" width="12" height="8" fill="#1a1a1a"/>
                
                <!-- ランプ本体 -->
                <path d="M 100 80 L 85 65 L 85 45 L 115 45 L 115 65 Z" 
                      fill="#3a3a3a" 
                      stroke="#2a2a2a" 
                      stroke-width="2"/>
                
                <!-- ランプのトップ -->
                <ellipse cx="100" cy="45" rx="15" ry="5" fill="#2a2a2a"/>
                
                <!-- ガラスカバー -->
                <path d="M 88 65 L 88 72 L 112 72 L 112 65" 
                      fill="rgba(200, 200, 220, 0.2)" 
                      stroke="#4a4a4a" 
                      stroke-width="1"/>
                
                <!-- 電球本体 -->
                <g class="bulb">
                    <!-- 電球のグロー -->
                    <ellipse cx="100" cy="60" rx="12" ry="10" 
                             fill="#ffb366" 
                             opacity="0.9"
                             filter="blur(3px)"/>
                    <ellipse cx="100" cy="60" rx="8" ry="7" 
                             fill="#ffe8cc" 
                             opacity="0.8"
                             filter="blur(2px)"/>
                    

                </g>
            </g>
        </svg>
    </div>


    
<style lang="scss">
        .streetlamp-container {
            position: relative;
            width: 400px;
            height: 600px;
        }

        /* 光のグロー効果 */
        .light-glow {
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, 
                rgba(255, 200, 100, 0.5) 0%,
                rgba(255, 170, 70, 0.25) 30%,
                rgba(255, 150, 50, 0.12) 50%,
                transparent 70%);
            filter: blur(30px);
            animation: flicker-glow 20s infinite ease-in-out;
            pointer-events: none;
        }

        /* 地面の光 */
        .ground-light {
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 400px;
            height: 150px;
            background: radial-gradient(ellipse at center, 
                rgba(255, 200, 100, 0.18) 0%,
                rgba(255, 170, 70, 0.1) 40%,
                transparent 70%);
            filter: blur(20px);
            animation: flicker-ground 20s infinite ease-in-out;
        }

        /* SVG街灯本体 */
        .streetlamp {
            position: relative;
            z-index: 10;
        }

        /* 電球部分 */
        .bulb {
            animation: flicker-bulb 20s infinite ease-in-out;
            transform-origin: center;
        }

        /* 街灯の揺れ */
        .pole {
            animation: sway 6s ease-in-out infinite;
            transform-origin: bottom center;
        }

        /* フリッカーアニメーション-グロー */
        @keyframes flicker-glow {
            0% {
                opacity: 0.7;
            }
            10% {
                opacity: 0.65;
            }
            20% {
                opacity: 0.4;
            }
            22% {
                opacity: 0.2;
            }
            24% {
                opacity: 0.45;
            }
            30% {
                opacity: 0.5;
            }
            50% {
                opacity: 0.75;
            }
            70% {
                opacity: 0.6;
            }
            85% {
                opacity: 0.3;
            }
            87% {
                opacity: 0.65;
            }
            100% {
                opacity: 0.7;
            }
        }

        /* フリッカーアニメーション - 地面 */
        @keyframes flicker-ground {
            0% {
                opacity: 0.6;
            }
            10% {
                opacity: 0.55;
            }
            20% {
                opacity: 0.35;
            }
            22% {
                opacity: 0.2;
            }
            24% {
                opacity: 0.4;
            }
            30% {
                opacity: 0.45;
            }
            50% {
                opacity: 0.65;
            }
            70% {
                opacity: 0.5;
            }
            85% {
                opacity: 0.25;
            }
            87% {
                opacity: 0.55;
            }
            100% {
                opacity: 0.6;
            }
        }

        /* フリッカーアニメーション - 電球 */
        @keyframes flicker-bulb {
            0% {
                opacity: 0.95;
                filter: brightness(1.1);
            }
            10% {
                opacity: 0.9;
                filter: brightness(1);
            }
            20% {
                opacity: 0.6;
                filter: brightness(0.7);
            }
            22% {
                opacity: 0.4;
                filter: brightness(0.5);
            }
            24% {
                opacity: 0.65;
                filter: brightness(0.75);
            }
            30% {
                opacity: 0.7;
                filter: brightness(0.8);
            }
            50% {
                opacity: 1;
                filter: brightness(1.15);
            }
            70% {
                opacity: 0.85;
                filter: brightness(0.95);
            }
            85% {
                opacity: 0.5;
                filter: brightness(0.6);
            }
            87% {
                opacity: 0.9;
                filter: brightness(1);
            }
            100% {
                opacity: 0.95;
                filter: brightness(1.1);
            }
        }

        /* 街灯の揺れ */
        @keyframes sway {
            0%, 100% {
                transform: rotate(0deg);
            }
            25% {
                transform: rotate(0.5deg);
            }
            50% {
                transform: rotate(0deg);
            }
            75% {
                transform: rotate(-0.5deg);
            }
        }

        /* パーティクル効果(電球付近に移動) */
        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: rgba(255, 240, 200, 0.6);
            border-radius: 50%;
            animation: float 3s infinite ease-in-out;
            filter: blur(1px);
        }

        .particle:nth-child(1) {
            top: 80px;
            left: 180px;
            animation-delay: 0s;
            animation-duration: 2.5s;
        }

        .particle:nth-child(2) {
            top: 90px;
            left: 220px;
            animation-delay: 0.5s;
            animation-duration: 3s;
        }

        .particle:nth-child(3) {
            top: 85px;
            left: 200px;
            animation-delay: 1s;
            animation-duration: 2.8s;
        }

        @keyframes float {
            0%, 100% {
                transform: translate(0, 0);
                opacity: 0.3;
            }
            25% {
                transform: translate(10px, -15px);
                opacity: 0.6;
            }
            50% {
                transform: translate(-5px, -5px);
                opacity: 0.4;
            }
            75% {
                transform: translate(15px, 10px);
                opacity: 0.7;
            }
        }

        /* 星 */
        .star {
            position: absolute;
            width: 2px;
            height: 2px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 50%;
            box-shadow: 0 0 3px rgba(255, 255, 255, 0.5);
            animation: twinkle 3s infinite ease-in-out;
        }

        .star:nth-child(4) {
            top: 50px;
            left: 80px;
            animation-delay: 0s;
            animation-duration: 2.8s;
        }

        .star:nth-child(5) {
            top: 120px;
            left: 320px;
            animation-delay: 0.7s;
            animation-duration: 3.2s;
        }

        .star:nth-child(6) {
            top: 180px;
            left: 60px;
            animation-delay: 1.2s;
            animation-duration: 2.5s;
        }

        .star:nth-child(7) {
            top: 90px;
            left: 340px;
            animation-delay: 1.8s;
            animation-duration: 3.5s;
        }

        .star:nth-child(8) {
            top: 140px;
            left: 100px;
            animation-delay: 0.4s;
            animation-duration: 3s;
        }

        .star:nth-child(9) {
            top: 70px;
            left: 260px;
            animation-delay: 2.1s;
            animation-duration: 2.7s;
        }

        @keyframes twinkle {
            0%, 100% {
                opacity: 0.3;
                transform: scale(1);
            }
            50% {
                opacity: 1;
                transform: scale(1.3);
            }
        }
</style>